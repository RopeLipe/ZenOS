# Python dependencies for the GTK installer
# Install with: pip install -r requirements.txt

# GTK4 bindings for Python
PyGObject>=3.42.0

# Additional utilities (may need system packages)
# On Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0
# On Fedora: sudo dnf install python3-gobject gtk4-devel
# On Arch: sudo pacman -S python-gobject gtk4
